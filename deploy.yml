name: Deploy Project

on:
  push:
    branches:
      - main
    run-on: ubuntu-latest
    container:
      image: node:20
    steps:
      - uses: actions/checkout@v3
        with: 
          node-version: 20
      - run: npn ci
      - run: npm run build